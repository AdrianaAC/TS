// https://kangax.github.io/compat-table/es6/  --> list of js features and what brownser and compilers supports it

const userName = "Consuela";
let age = 31;

const add = (n1: number, n2: number) => {
  return n1 + n2;
};

console.log(add(2.7, 89));
