let userName = "Consuela";
console.log(userName);
//--watch or -w to get watch mode on one file
//tsc --init to start a project and watch the whole project(everything in the folder where the command runs is a project and should be managed by ts). Only required once
